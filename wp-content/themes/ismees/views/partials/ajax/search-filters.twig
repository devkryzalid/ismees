<form id="ajax-form" class="search-filters-form">
    <input type="hidden" id="ajax-settings" data-url="{{ url }}">
    <div class="search-input-ctn">
        <div class="input-ctn">
            <input type="text" placeholder="Rechercher" />
        </div>
    </div>
    <div class="subfilters-ctn">
        <div id="togglerContainer" data-aos="fade-left" data-aos-delay="300">
            <label for="filtersToggler" id="unchecked-label">{{ __('Étudiant.es', 'ismees') }}</label>
            <input type="checkbox" name="for_members" id="filtersToggler" {% if for_members %}checked{% endif %}>
            <label for="filtersToggler" id="checked-label">{{ __('Personnel des établissements', 'ismees') }}</label>
        </div>
        <div class="">
            {# <div class="">
                <a href="{{ item.url }}"{% if item.children %}class="has-child-link"{% endif %}>{% if item.children %}<i class="icon-simple-arrow-right"></i>{% endif %}{{ item.title }}</a>
                {% if item.children %}
                        <ul class="child-menu">
                            {% for subItem in item.children %}
                                <li class="{{ subItem.classes | join(' ') }} subItem {{ subItem.children ? 'has-sub-child' : '' }}">
                                    <a target="{{ subItem.target }}" {% if subItem.children %}class="has-sub-child-title" tabindex="-1"{% endif %} href="{{ subItem.link }}">{{ subItem.title }}</a>
                                    {% if subItem.children %}
                                        <ul class="subChild-menu">
                                            {% for lastItem in subItem.children %}
                                                <li class="{{ lastItem.classes | join(' ') }} lastItem">
                                                    <a target="{{ lastItem.target }}" href="{{ lastItem.link }}">{{ lastItem.title }}</a>
                                                </li>
                                            {% endfor %}
                                        </ul>
                                    {% endif %}
                                </li>
                            {% endfor %}
                        </ul>
                {% endif %}
            </div> #}
        </div>
    </div>
</form>
